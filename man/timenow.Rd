% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timenow.R
\name{timenow}
\alias{timenow}
\title{Get current time with timezone awareness}
\usage{
timenow(tz = NULL, quiet = FALSE)
}
\arguments{
\item{tz}{Optional timezone specification. Can be:
\itemize{
\item NULL (default): show UTC and detected local timezone
\item A valid IANA timezone name: "Australia/Hobart"
\item A fuzzy string: "Perth Australia", "new york", "tokyo"
}}

\item{quiet}{Suppress the local timezone detection message}
}
\value{
A named list of class "timenow" with UTC and local times (invisibly
}
\description{
Reports the current timestamp in UTC and optionally in a local or specified
timezone. Uses a detection cascade to find your local timezone when the
system reports UTC.
}
\details{
\subsection{Timezone detection cascade}{

When your system timezone is UTC (common on servers), timenow uses this
cascade to find your actual local timezone:
\enumerate{
\item \code{getOption("timenow.tz")} - set in .Rprofile
\item \code{Sys.getenv("R_TIMENOW_TZ")} - set in .Renviron
\item \verb{/etc/timezone} file (Debian/Ubuntu)
\item \code{timedatectl} output (systemd)
\item \code{Sys.timezone()} - the R default
}
}

\subsection{Setting your timezone}{

In \verb{~/.Renviron}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{R_TIMENOW_TZ=Australia/Hobart
}\if{html}{\out{</div>}}

Or in \verb{~/.Rprofile}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{options(timenow.tz = "Australia/Hobart")
}\if{html}{\out{</div>}}
}
}
\examples{
timenow()
timenow("Perth")
timenow("US Eastern")
}
